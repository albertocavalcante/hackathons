name: 'Terraform Apply'

on:
  push:
    branches:
      - main
    paths:
      - '2025-06-29-modular-hack-weekend/infrastructure/**'
      - '.github/workflows/terraform-*.yml'

permissions:
  contents: read
  pull-requests: write
  issues: write

env:
  # Terraform Cloud Configuration
  TERRAFORM_CLOUD_TOKENS: app.terraform.io=${{ secrets.TF_API_TOKEN }}
  TF_CLOUD_ORGANIZATION: "alberto"
  TF_WORKSPACE: "modular-hackathon-jun-2025"

  # Provider Configuration
  LAMBDALABS_API_KEY: ${{ secrets.LAMBDALABS_API_KEY }}

  # GitHub Actions Configuration
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Project Configuration
  TERRAFORM_PATH: "2025-06-29-modular-hack-weekend/infrastructure"

jobs:
  terraform-apply:
    name: 'Terraform Apply'
    runs-on: ubuntu-latest
    environment: production

    steps:
    - name: Checkout
      uses: actions/checkout@v5

    - name: Terraform fmt check
      uses: dflook/terraform-github-actions/terraform-fmt-check@f8daa870ddadd8c32950e475f494e40defc0a20a # v2.2.3
      with:
        path: ${{ env.TERRAFORM_PATH }}

    - name: Terraform validate
      uses: dflook/terraform-github-actions/terraform-validate@f8daa870ddadd8c32950e475f494e40defc0a20a # v2.2.3
      with:
        path: ${{ env.TERRAFORM_PATH }}

    - name: Terraform apply
      id: apply
      uses: dflook/terraform-github-actions/terraform-apply@f8daa870ddadd8c32950e475f494e40defc0a20a # v2.2.3
      with:
        path: ${{ env.TERRAFORM_PATH }}
        workspace: ${{ env.TF_WORKSPACE }}
        auto_approve: true

    - name: Get Terraform outputs
      id: output
      uses: dflook/terraform-github-actions/terraform-output@f8daa870ddadd8c32950e475f494e40defc0a20a # v2.2.3
      with:
        path: ${{ env.TERRAFORM_PATH }}
        workspace: ${{ env.TF_WORKSPACE }}

    - name: Comment Results
      uses: actions/github-script@v8
      with:
        script: |
          const output = `### 🚀 Infrastructure Deployed Successfully!

          **Instance Details:**
          - **IP**: ${{ steps.output.outputs.instance_ip }}
          - **ID**: ${{ steps.output.outputs.instance_id }}
          - **Type**: ${{ steps.output.outputs.instance_type }}

          **Next Steps:**
          \`\`\`bash
          # Connect to instance
          cd 2025-06-29-modular-hack-weekend
          make ssh

          # Monitor status
          make status

          # When done, clean up resources
          make destroy
          \`\`\`

          💰 **Remember**: Destroy resources when not in use to avoid charges!
          `;

          github.rest.repos.createCommitComment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            commit_sha: context.sha,
            body: output
          });
